# راهنمای استفاده از تانل Vxlan

## نحوه استفاده از ربات برای نصب Vxlan

### مرحله 1: شروع کار
1. به ربات تلگرام پیام `/start` بفرستید
2. روی دکمه "نصب تانل" کلیک کنید
3. روی دکمه "Vxlan" کلیک کنید

### مرحله 2: اطلاعات سرور ایران
ربات از شما اطلاعات زیر را درخواست میکنه:

\`\`\`
IPin: 5.5.5.5
User: root
Pass: mypassword
SSH Port: 22
Tunneltype: Reverse
Tunnelname: Ahmad75
Iptype: IPv4
Tunnelport: 50820
Transport: TCP
Tcpnodelay: true
Securitytoken: mytoken123
Serviceports: 8090,4830,3333
\`\`\`

**توضیحات:**
- `IPin`: آیپی سرور ایران (سرور اول)
- `User`: نام کاربری SSH
- `Pass`: رمز عبور SSH
- `SSH Port`: پورت SSH (معمولاً 22)
- `Tunneltype`: Direct یا Reverse
- `Tunnelname`: نام دلخواه برای تانل
- `Iptype`: IPv4 یا IPv6
- `Tunnelport`: پورت تانل (باید با پورت‌های سرویس متفاوت باشه)
- `Transport`: TCP یا UDP (کوچک یا بزرگ فرقی نداره، ربات تبدیل میکنه)
- `Tcpnodelay`: true یا false
- `Securitytoken`: رمز امنیتی (باید با سرور خارج یکسان باشه)
- `Serviceports`: پورت‌های سرویس با کاما جدا شده

### مرحله 3: اطلاعات سرور خارج
ربات از شما اطلاعات زیر را درخواست میکنه:

\`\`\`
IPout: 6.6.6.6
User: root
Pass: mypassword2
SSH Port: 22
Tunneltype: Reverse
Tunnelname: Ahmad75
Iranip: 5.5.5.5
Tunnelport: 50820
Transport: TCP
Tcpnodelay: true
Securitytoken: mytoken123
Serviceports: 8090,4830,3333
\`\`\`

**نکات مهم:**
- تمام پارامترها (Tunnelname, Tunnelport, Transport, Securitytoken, Serviceports) باید با سرور ایران یکسان باشند
- `Iranip`: آیپی سرور ایران که قبلاً وارد کردید
- `IPout`: آیپی سرور خارج

### مرحله 4: نصب خودکار
ربات به صورت خودکار:
1. به سرور ایران متصل میشه
2. اسکریپت نصب RGT رو آپلود و اجرا میکنه
3. به سرور خارج متصل میشه
4. اسکریپت نصب RGT رو آپلود و اجرا میکنه
5. اطلاعات تانل رو در دیتابیس ذخیره میکنه
6. یک Tunnel ID منحصر به فرد برای شما تولید میکنه

### مرحله 5: دریافت نتیجه
در صورت موفقیت، پیام زیر رو دریافت میکنید:

\`\`\`
✅ تانل Vxlan با موفقیت نصب شد!

🆔 Tunnel ID: `TUN-ABC123XYZ`

📋 اطلاعات تانل:
🇮🇷 IPin: 5.5.5.5
🌍 IPout: 6.6.6.6
🔌 Serviceports: 8090,4830,3333

✨ تمام اطلاعات برای مدیریت بعدی ذخیره شده است.
\`\`\`

## عیب‌یابی

### خطای اتصال SSH
اگر پیام "❌ خطا در نصب بر روی سرور ایران" یا "❌ خطا در نصب بر روی سرور خارج" دریافت کردید:
- مطمئن بشید اطلاعات SSH صحیح هستند
- چک کنید که فایروال سرور پورت SSH رو باز کرده باشه
- مطمئن بشید که به اینترنت دسترسی دارید

### خطای پارامترها
اگر پیام "❌ فیلدهای زیر الزامی هستند" دریافت کردید:
- مطمئن بشید تمام فیلدها رو وارد کردید
- فرمت ورودی باید دقیقاً مثل مثال باشه (Key: Value)

### بررسی وضعیت سرویس
برای بررسی وضعیت سرویس روی سرور:

**سرور ایران:**
\`\`\`bash
systemctl status RGT-iran-Ahmad75.service
\`\`\`

**سرور خارج:**
\`\`\`bash
systemctl status RGT-kharej-Ahmad75.service
\`\`\`

### مشاهده لاگ‌ها
برای مشاهده لاگ‌های سرویس:

**سرور ایران:**
\`\`\`bash
journalctl -u RGT-iran-Ahmad75.service -f
\`\`\`

**سرور خارج:**
\`\`\`bash
journalctl -u RGT-kharej-Ahmad75.service -f
\`\`\`

## فایل‌های نصب شده

### سرور ایران
- `/root/rgt-core/rgt` - فایل اجرایی RGT
- `/root/rgt-core/iran-Ahmad75.toml` - فایل کانفیگ
- `/etc/systemd/system/RGT-iran-Ahmad75.service` - سرویس systemd

### سرور خارج
- `/root/rgt-core/rgt` - فایل اجرایی RGT
- `/root/rgt-core/kharej-Ahmad75.toml` - فایل کانفیگ
- `/etc/systemd/system/RGT-kharej-Ahmad75.service` - سرویس systemd

## حذف تانل

برای حذف تانل از سرور:

**سرور ایران:**
\`\`\`bash
systemctl stop RGT-iran-Ahmad75.service
systemctl disable RGT-iran-Ahmad75.service
rm /etc/systemd/system/RGT-iran-Ahmad75.service
rm /root/rgt-core/iran-Ahmad75.toml
systemctl daemon-reload
\`\`\`

**سرور خارج:**
\`\`\`bash
systemctl stop RGT-kharej-Ahmad75.service
systemctl disable RGT-kharej-Ahmad75.service
rm /etc/systemd/system/RGT-kharej-Ahmad75.service
rm /root/rgt-core/kharej-Ahmad75.toml
systemctl daemon-reload
